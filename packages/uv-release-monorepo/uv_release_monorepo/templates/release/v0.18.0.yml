name: Release Wheels
on:
  workflow_dispatch:
    inputs:
      plan:
        description: Release plan JSON (generated by uvr release).
        type: string
        required: true
permissions:
  contents: write
jobs:
  validate_plan:
    runs-on: ubuntu-latest
    steps:
    - id: setup-uv
      uses: astral-sh/setup-uv@v5
      with:
        python-version: ${{ fromJSON(inputs.plan).python_version }}
    - id: validate-plan
      name: Validate release plan
      env:
        UVR_PLAN: ${{ inputs.plan }}
      run: uv run uvr validate-plan
  build:
    runs-on: ${{ matrix.runner }}
    if: ${{ !contains(fromJSON(inputs.plan).skip, 'build') }}
    needs:
    - validate_plan
    strategy:
      fail-fast: true
      matrix:
        runner: ${{ fromJSON(inputs.plan).build_matrix }}
    steps:
    - id: checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - id: setup-uv
      uses: astral-sh/setup-uv@v5
      with:
        python-version: ${{ fromJSON(inputs.plan).python_version }}
    - id: build
      name: Build packages
      env:
        GH_TOKEN: ${{ github.token }}
        UVR_PLAN: ${{ inputs.plan }}
      run: uv run uvr build --runner '${{ toJSON(matrix.runner) }}'
    - id: upload
      uses: actions/upload-artifact@v4
      with:
        name: wheels-${{ join(matrix.runner, '-') }}
        path: dist/*.whl
        if-no-files-found: error
  release:
    runs-on: ubuntu-latest
    if: ${{ always() && !failure() && !contains(fromJSON(inputs.plan).skip, 'release') }}
    needs:
    - build
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(inputs.plan).release_matrix }}
    steps:
    - id: download
      uses: actions/download-artifact@v4
      with:
        pattern: wheels-*
        merge-multiple: true
        path: dist
        run-id: ${{ fromJSON(inputs.plan).reuse_run_id != '' && fromJSON(inputs.plan).reuse_run_id || github.run_id }}
    - id: publish
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ matrix.tag }}
        name: ${{ matrix.title }}
        body: ${{ matrix.body }}
        files: dist/${{ matrix.dist_name }}-${{ matrix.version }}*.whl
        make_latest: ${{ matrix.make_latest }}
  finalize:
    runs-on: ubuntu-latest
    if: ${{ always() && !failure() && !contains(fromJSON(inputs.plan).skip, 'finalize') }}
    needs:
    - release
    steps:
    - id: checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - id: setup-uv
      uses: astral-sh/setup-uv@v5
      with:
        python-version: ${{ fromJSON(inputs.plan).python_version }}
    - id: finalize
      name: Finalize release
      env:
        UVR_PLAN: ${{ inputs.plan }}
      run: uv run uvr finalize
