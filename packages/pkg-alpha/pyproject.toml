[project]
name = "pkg-alpha"
version = "0.1.8"
description = "Dummy package alpha - no dependencies"
requires-python = ">=3.12"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pkg_alpha"]
